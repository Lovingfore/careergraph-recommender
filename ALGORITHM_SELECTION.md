# CareerGraph Recommender 算法选择说明

## 选定方案

课程版先采用“内容匹配 + 职位转移图”的混合推荐算法：

```text
推荐总分 = 0.45 * Match - 0.25 * Gap + 0.15 * Growth + 0.15 * Path
```

- `Match`：用户技能向量与目标职位需求向量的余弦相似度。
- `Gap`：目标职位要求超过用户当前技能水平的加权缺口。
- `Growth`：目标职位相对当前职位的技能要求提升空间。
- `Path`：当前职位在职位转移图上到目标职位的最大路径概率。

推荐结果按总分降序输出 Top-K，并同时显示缺口技能、预计补全时间和路径节点。

## 技能预测方案

当前数据规模较小，第一周保留每项技能的线性趋势 baseline 预测未来 6 个月水平。它的优点是数据要求低、易解释、便于做误差评价。同时，`src/temporal_gat.py` 已提供“技能时间序列 GRU + 多头邻居聚合”的 TemporalGAT 最小前向传播骨架，输入输出接口与 DataLoader 对齐；第二周再补充训练集划分、损失函数、指标和调参，不把未训练的骨架当作已验证模型结果。

## 第1周工程闭环

`src/database.py` 将清洗后的六张 CSV 表导入 SQLite，并通过主键、外键、联合主键和索引保证数据关系可查询。`src/data_loader.py` 将连续月份补零后生成 `[batch, time, skill]` 窗口，另外输出职位-技能边、职位转移边和技能共现边张量。`web/` 只调用这些共享服务，页面 `/` 与接口 `/api/summary/` 用于展示行数、Loader 状态和样例用户画像。

## 为什么不直接使用复杂模型

当前数据只有 12 个职位、35 个技能和 6 个教学用户。直接使用时序图注意力网络容易过拟合，且难以解释。混合推荐算法不需要大规模用户行为数据，能够先验证职位-技能图、特征工程、推荐排序、路径搜索和 Web 展示的完整链路。

## 评价方法

- 推荐排序：Precision@K、Recall@K、NDCG@K。
- 技能预测：MAE、RMSE。
- 路径规划：Path Feasibility、预计补全时间、路径长度。
- 优化比较：相对“仅 Match”的 baseline 计算 `Delta Metric = Metric_optimized - Metric_baseline`。

## 数据限制

O*NET 职位和技能表属于公开真实数据。`user_skill_events.csv` 与 `job_transitions.csv` 是为了让课程原型可运行而生成的确定性教学数据，报告中必须明确注明这一点。后续获得真实简历或职业转移数据后，可以直接替换这两个文件，不需要修改特征字段和推荐接口。
